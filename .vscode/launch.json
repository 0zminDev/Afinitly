{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Nx Serve All",
			"type": "node",
			"request": "launch",
			"runtimeExecutable": "npx",
			"args": ["nx", "run-many", "--target=serve"],
			"cwd": "${workspaceFolder}",
			"console": "integratedTerminal"
		}
	]
}
